Searches the project for a desired file

Usage: **<%= cli.bin %> search** *[options]*

Options:

  *--copy*    *....* copies the desired file to the clipboard

  *--open app-name*    *....* opens the desired file with the specified *app-name*