Catalog the component structure to an external service/backend

Usage: **<%= cli.bin %> catalog** *[options]*

Options:

  *--output-file*    *....* name of the file to putput the results to.

  *--catalog-url*    *....* URL endpoint to POST the modules info to.
